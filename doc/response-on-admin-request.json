{ // response on amdin request, e.g. create or destroy a database.
  // create new database, with request argument(quiet = 1(default)).
    "code": 200,
}

{ // response on amdin request, e.g. create or destroy a database.
  // create new database, with request argument(quiet = 0).
    "code": 200,
    "status": "OK",
    "message": "New database done.",
    "date": "2012-12-12 12:12:12",
    "options": {
        "dbname": "default",
        "create_if_missing": true,
        "other-items": "values"
    }
}
